// Client portal venues: initializes the public-style venue list within the client portal.
document.addEventListener('DOMContentLoaded', function () {
            VenuesPages.init({ mode: 'portal', page: 'list' });
        });
